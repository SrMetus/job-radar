# Replace placeholders with your local PostgreSQL connection details.
# URL-encode special characters in the username and password.
DATABASE_URL=postgresql+psycopg://<username>:<password>@localhost:5432/job_radar
